# Matching Engine

**Setup Instructions**
1. Make sure uv is installed on device.
2. Open the terminal inside stock-market-simulation/matching_engine.
3. Run `uv sync`.

**PR**
1. Run `make check-all` to ensure linting, formatting and testing passes.
